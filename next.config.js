/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  images: { unoptimized: true },
  basePath: '/AdProtfolio',
  assetPrefix: '/AdProtfolio/',
};

module.exports = nextConfig;